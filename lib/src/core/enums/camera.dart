/// The device camera.
enum Camera {
  /// The device's front camera.
  frontCamera,

  /// The device's back camera.
  backCamera,

  /// The external camera connected to the device.
  externalCamera,
}
