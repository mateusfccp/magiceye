/// The device camera.
enum Camera {
  /// The device front camera.
  FrontCamera,

  /// The device back camera.
  BackCamera,

  /// The external camera connected to the device.
  ExternalCamera,
}
